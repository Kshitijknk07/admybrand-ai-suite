import type { Metadata } from "next";
import { Space_Grotesk, Inter } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";
import { ThemeProvider } from "@/components/ThemeProvider";

const spaceGrotesk = Space_Grotesk({
  variable: "--font-space-grotesk",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
});

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
});

export const metadata: Metadata = {
  title: "ADmyBRAND AI Suite - Revolutionary AI Marketing Platform",
  description: "Transform your marketing with AI-powered insights, automation, and optimization. The future of digital marketing is here.",
  keywords: ["AI marketing", "digital marketing", "automation", "analytics", "brand optimization"],
  authors: [{ name: "ADmyBRAND AI Suite" }],
  openGraph: {
    title: "ADmyBRAND AI Suite - Revolutionary AI Marketing Platform",
    description: "Transform your marketing with AI-powered insights, automation, and optimization.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${spaceGrotesk.variable} ${inter.variable} antialiased`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
